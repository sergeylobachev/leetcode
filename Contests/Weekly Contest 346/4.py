from collections import defaultdict

def solution(n, edges, source, destination, target):

    # graph = defaultdict(list)
    # for s, d, w in edges:
    #     graph[s].append((d, w))
    #     graph[d].append((s, w))

    # parents = [-1] * n
    # visited = [-1] * n

    # q = []
    # q.append(source)
    
    # while q:
    #     u = q.pop(0)
    #     visited[u] = 1
    #     for v, w in graph[u]:
    #         if v not in visited:
    #             q.append(v)
    #             parent[v] = u
    #             if v == destination:
    #                 break

    



    